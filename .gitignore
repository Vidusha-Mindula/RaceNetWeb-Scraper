bin/
obj/
*.user
.vs/

# Build/publish output — regenerated by installer/build-racenet-installer.ps1, never
# hand-edited, and far too large to check in.
installer/publish-racenet/
installer/output/

# Server-local secrets — written by the app itself at runtime, never meant to leave the
# machine it's on.
settings.json
